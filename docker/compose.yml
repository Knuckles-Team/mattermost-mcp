services:
  mattermost-mcp:
    build: .
    environment:
      - MATTERMOST_URL
      - MATTERMOST_TOKEN
      - TRANSPORT=streamable-http
      - HOST=0.0.0.0
      - PORT=8000
    ports:
      - "8000:8000"
